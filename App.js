const heading = React.createElement("h1", { title: "this is React you know?", id: "heading" }, "Namaste Akshay from React.");

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(heading);